<div class="container mt-5 text-center">
    <h1 class="text-success">Order Confirmed!</h1>
    <p class="lead">Thank you for your purchase, <strong>
            <%= order.customerName %>
        </strong>.</p>
    <div class="card mx-auto" style="max-width: 500px;">
        <div class="card-body">
            <h5 class="card-title">Order ID: <%= order.orderCode || order._id %>
            </h5>
            <p class="card-text">Total Amount: <strong>$<%= order.totalAmount %></strong></p>
            <p class="text-muted">A confirmation email has been sent to <%= order.email %>.</p>
            <a href="/products" class="btn btn-primary">Continue Shopping</a>
        </div>
    </div>
</div>